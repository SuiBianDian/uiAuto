"""
右键运行和目录运行是不一样的，
右键运行：在根目录，环境变量（lib—库 也被添加到环境变量了）里寻找包
命令运行：只在环境变量里寻找包，所以必须将当前目录地址加进去，就会在当前目录下 + 同级目录中寻找
"""


"""
注意此处的文件地址，因为我用的命令行执行，所以是uiAuto目录下，那么直接进入data就行了。
但是，如果是用的点击运行，那就不行了，那此时是在scripts目录下，想进入data需要返回上一级在进入
"""

jenkins也用的命令行执行，所以应该按照命令行的标准来，并且jenkins是将整个project下载到本地worker中，所以语句还是正确的。

"""必须等待，应为他是一个动态的出现过程，找到了还不能立刻点击"""